<%= simple_form_for([ @product, Review.new ], data: { insert_in_list_target: "form",
                                                action: 'submit->insert-in-list#send' }) do |f| %>
    <%= f.input :content %>
    <%= f.button :submit, class: "btn-outline-secondary mx-3" %>
<% end %>